(ns {{namespace}}-clj-test
  (:require [clojure.test :refer :all]
            [{{namespace}}.server :as server]
            [{{namespace}}.handler :as handler]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
